!!!

%html{lang: 'de'}
  %head
    %meta{charset: 'utf-8'}
    = csrf_meta_tags
    %title= 'rylyty'

    -# Include Stylesheets
    -#
    = stylesheet_link_tag 'game_editor'
    -#Enabling bootstrap's responsive features
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
    = javascript_include_tag 'active_admin'

  %body
    %div
      - if flash[:alert].present?
        %div.alert.alert-error
          = flash[:alert]

      - if flash[:notice].present?
        %div.alert.alert-info
          = flash[:notice]
    = yield
